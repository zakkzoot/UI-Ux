interface:
  display_name: "UI Ux"
  short_description: "Repo-specific patterns and workflows for UI-Ux"
  default_prompt: "Use the UI-Ux repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true